# 提交一个案例

欢迎任何 AI 视频模型、任意发布平台的作品；允许模型未公开和多工具组合。请提供作品原链接、作者主页，以及可核对的 AI 创作依据。

## 无需写代码

在仓库 Issues 中选择“提交视频案例”，填写作品和 Prompt 来源。没有找到 Prompt 也可以提交，审核后会归入灵感区。不要根据视频自行反推后填成作者原文。

## 通过 Pull Request 投稿

1. 使用 `npm run new -- '原帖URL' '平台' '标题'` 创建候选记录。
2. 在 `data/candidates/` 补充公开信息。只提交有依据的模型、版本、参数和 Prompt。
3. 执行 `npm run build` 和 `npm run verify`，一并提交 JSON 及生成文件。
4. 维护者检查证据、原文范围和重复情况后，把记录移到 `data/cases/` 并更新审核状态及日期。

如果您是原作者，欢迎补充允许引用文本或展示预览图的范围。不要上传未经授权的视频或参考图片。候选文件也在公开仓库内，不要放私信、邮箱、API key 或其他非公开信息。

## 什么是可用的证据

原帖、作者回复、作者自己的制作说明均可。第三方整理库可帮助发现作品，单独依靠它不足以审核通过。注明 Prompt 在正文、哪个镜头、哪个评论或教程的什么位置。

模型名称不能凭所在社区推断；一个帖子发在某模型社区，作者仍可能用其他模型。日期不确定时填 `null`，不得把检索日期填成作品发布日期。

## 纠错与移除

使用“纠错或移除” Issue 模板，提供案例编号或链接、问题描述和可以公开的依据。维护者修改源 JSON 后重新生成；删除案例后还应执行 `build` 清理已生成页面。详见 [RIGHTS.md](RIGHTS.md)。
